@extends('admin.layouts.app')

@section('title', 'Categories')
@section('page-title', 'Category Management')
@section('page-description', 'Organize your product categories')

@section('content')
<div class="animate-fade-in-up">
    <!-- Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-blue-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Total Categories</p>
                    <h3 class="text-3xl font-bold text-white">{{ count($categories) }}</h3>
                </div>
                <div class="w-14 h-14 bg-blue-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-tags text-blue-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-green-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Active Categories</p>
                    <h3 class="text-3xl font-bold text-white">{{ count($categories) }}</h3>
                </div>
                <div class="w-14 h-14 bg-green-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-check-circle text-green-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-purple-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Products Listed</p>
                    <h3 class="text-3xl font-bold text-white">{{ $categories->sum('products_count') }}</h3>
                </div>
                <div class="w-14 h-14 bg-purple-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-box text-purple-400 text-2xl"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Categories Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        @foreach($categories as $category)
        <div class="glass-effect rounded-2xl overflow-hidden card-hover group">
            <div class="h-48 bg-gradient-to-br from-blue-500/20 to-purple-500/20 flex items-center justify-center border-b border-slate-800">
                <i class="fas fa-tags text-6xl text-blue-400/50 group-hover:text-blue-400 transition-all group-hover:scale-110"></i>
            </div>
            
            <div class="p-6">
                <div class="flex items-start justify-between mb-3">
                    <div class="flex-1">
                        <h3 class="text-xl font-bold text-white mb-1">{{ $category->name }}</h3>
                        <p class="text-sm text-slate-400">{{ $category->products_count }} products</p>
                    </div>
                    <span class="px-3 py-1 bg-blue-500/20 text-blue-400 rounded-lg text-xs font-semibold border border-blue-500/30">
                        Active
                    </span>
                </div>
                
                <p class="text-slate-400 text-sm mb-4 line-clamp-2">{{ $category->description ?? 'No description available' }}</p>
                
                <div class="flex items-center space-x-2">
                    <button class="flex-1 px-4 py-2 bg-blue-500/20 text-blue-400 rounded-lg hover:bg-blue-500/30 border border-blue-500/30 hover:border-blue-500/50 transition-all font-medium">
                        <i class="fas fa-edit mr-2"></i>Edit
                    </button>
                    <button class="px-4 py-2 bg-red-500/20 text-red-400 rounded-lg hover:bg-red-500/30 border border-red-500/30 hover:border-red-500/50 transition-all">
                        <i class="fas fa-trash"></i>
                    </button>
                </div>
            </div>
        </div>
        @endforeach
        
        <!-- Add New Category Card -->
        <div class="glass-effect rounded-2xl overflow-hidden border-2 border-dashed border-slate-700 hover:border-blue-500 transition-all cursor-pointer group">
            <div class="h-full flex flex-col items-center justify-center p-8 text-center">
                <div class="w-20 h-20 bg-blue-500/20 rounded-2xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform">
                    <i class="fas fa-plus text-3xl text-blue-400"></i>
                </div>
                <h3 class="text-lg font-bold text-white mb-2">Add New Category</h3>
                <p class="text-sm text-slate-400">Create a new product category</p>
            </div>
        </div>
    </div>
</div>
@endsection
