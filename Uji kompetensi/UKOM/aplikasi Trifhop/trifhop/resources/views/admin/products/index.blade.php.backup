@extends('admin.layouts.app')

@section('title', 'Products')
@section('page-title', 'Product Inventory')
@section('page-description', 'Manage your thrift shop products')

@section('header-actions')
    <a href="{{ route('admin.products.create') }}" class="px-6 py-2.5 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white rounded-xl font-semibold shadow-lg shadow-blue-500/30 transition-all flex items-center space-x-2">
        <i class="fas fa-plus"></i>
        <span>Add Product</span>
    </a>
@endsection

@section('content')
<div class="animate-fade-in-up">
    <!-- Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-blue-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Total Products</p>
                    <h3 class="text-3xl font-bold text-white">{{ $totalProducts }}</h3>
                </div>
                <div class="w-14 h-14 bg-blue-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-box text-blue-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-green-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">In Stock</p>
                    <h3 class="text-3xl font-bold text-white">{{ $inStock }}</h3>
                </div>
                <div class="w-14 h-14 bg-green-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-check-circle text-green-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-yellow-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Low Stock</p>
                    <h3 class="text-3xl font-bold text-white">{{ $lowStock }}</h3>
                </div>
                <div class="w-14 h-14 bg-yellow-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-exclamation-triangle text-yellow-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-red-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Out of Stock</p>
                    <h3 class="text-3xl font-bold text-white">{{ $outOfStock }}</h3>
                </div>
                <div class="w-14 h-14 bg-red-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-times-circle text-red-400 text-2xl"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Products Table -->
    <div class="glass-effect rounded-2xl overflow-hidden">
        <div class="p-6 border-b border-slate-800">
            <div class="flex items-center justify-between">
                <h2 class="text-xl font-bold text-white">All Products</h2>
                <div class="flex items-center space-x-3">
                    <input type="text" placeholder="Search products..." class="px-4 py-2 bg-slate-800/50 border border-slate-700 rounded-xl text-white placeholder-slate-500 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 transition-all">
                    <button class="px-4 py-2 bg-slate-800/50 border border-slate-700 rounded-xl text-slate-400 hover:text-white hover:border-slate-600 transition-all">
                        <i class="fas fa-filter"></i>
                    </button>
                </div>
            </div>
        </div>
        
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-slate-900/50">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Product</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Category</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Price</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Stock</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Status</th>
                        <th class="px-6 py-4 text-right text-xs font-semibold text-slate-400 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-800">
                    @forelse($products as $product)
                    <tr class="hover:bg-slate-900/30 transition-colors">
                        <td class="px-6 py-4">
                            <div class="flex items-center space-x-4">
                                <img src="{{ asset($product->image_url) }}" alt="{{ $product->name }}" class="w-16 h-16 rounded-xl object-cover border-2 border-slate-700">
                                <div>
                                    <p class="font-semibold text-white">{{ $product->name }}</p>
                                    <p class="text-sm text-slate-400">{{ Str::limit($product->description, 40) }}</p>
                                </div>
                            </div>
                        </td>
                        <td class="px-6 py-4">
                            <span class="px-3 py-1.5 bg-blue-500/20 text-blue-400 rounded-lg text-sm font-medium border border-blue-500/30">
                                {{ $product->category->name }}
                            </span>
                        </td>
                        <td class="px-6 py-4">
                            <p class="font-bold text-white">Rp {{ number_format($product->price, 0, ',', '.') }}</p>
                        </td>
                        <td class="px-6 py-4">
                            <div class="flex items-center space-x-2">
                                <div class="w-2 h-2 rounded-full {{ $product->stock > 5 ? 'bg-green-400' : ($product->stock > 0 ? 'bg-yellow-400' : 'bg-red-400') }}"></div>
                                <span class="font-semibold {{ $product->stock > 5 ? 'text-green-400' : ($product->stock > 0 ? 'text-yellow-400' : 'text-red-400') }}">
                                    {{ $product->stock }} units
                                </span>
                            </div>
                        </td>
                        <td class="px-6 py-4">
                            @if($product->stock > 5)
                                <span class="px-3 py-1.5 bg-green-500/20 text-green-400 rounded-lg text-sm font-medium border border-green-500/30">In Stock</span>
                            @elseif($product->stock > 0)
                                <span class="px-3 py-1.5 bg-yellow-500/20 text-yellow-400 rounded-lg text-sm font-medium border border-yellow-500/30">Low Stock</span>
                            @else
                                <span class="px-3 py-1.5 bg-red-500/20 text-red-400 rounded-lg text-sm font-medium border border-red-500/30">Out of Stock</span>
                            @endif
                        </td>
                        <td class="px-6 py-4 text-right">
                            <div class="flex items-center justify-end space-x-2">
                                <a href="{{ route('admin.products.edit', $product) }}" class="px-3 py-2 bg-blue-500/20 text-blue-400 rounded-lg hover:bg-blue-500/30 border border-blue-500/30 hover:border-blue-500/50 transition-all">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form action="{{ route('admin.products.destroy', $product) }}" method="POST" onsubmit="return confirm('Are you sure?')">
                                    @csrf
                                    @method('DELETE')
                                    <button type="submit" class="px-3 py-2 bg-red-500/20 text-red-400 rounded-lg hover:bg-red-500/30 border border-red-500/30 hover:border-red-500/50 transition-all">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    @empty
                    <tr>
                        <td colspan="6" class="px-6 py-12 text-center">
                            <i class="fas fa-box-open text-6xl text-slate-700 mb-4"></i>
                            <p class="text-slate-400 text-lg">No products found</p>
                        </td>
                    </tr>
                    @endforelse
                </tbody>
            </table>
        </div>
        
        <div class="px-6 py-4 border-t border-slate-800">
            {{ $products->links() }}
        </div>
    </div>
</div>
@endsection
