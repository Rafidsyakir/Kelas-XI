@extends('admin.layouts.app')

@section('title', 'Customers')
@section('page-title', 'Customer Management')
@section('page-description', 'View and manage your customers')

@section('content')
<div class="animate-fade-in-up">
    <!-- Stats Overview -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-blue-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Total Customers</p>
                    <h3 class="text-3xl font-bold text-white">{{ $totalCustomers }}</h3>
                </div>
                <div class="w-14 h-14 bg-blue-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-users text-blue-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-green-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Active Today</p>
                    <h3 class="text-3xl font-bold text-white">{{ $activeToday }}</h3>
                </div>
                <div class="w-14 h-14 bg-green-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-user-check text-green-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-purple-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">New This Month</p>
                    <h3 class="text-3xl font-bold text-white">{{ $newThisMonth }}</h3>
                </div>
                <div class="w-14 h-14 bg-purple-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-user-plus text-purple-400 text-2xl"></i>
                </div>
            </div>
        </div>
        
        <div class="glass-effect rounded-2xl p-6 border-l-4 border-cyan-500">
            <div class="flex items-center justify-between">
                <div>
                    <p class="text-slate-400 text-sm font-medium mb-1">Total Orders</p>
                    <h3 class="text-3xl font-bold text-white">{{ $totalOrders }}</h3>
                </div>
                <div class="w-14 h-14 bg-cyan-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-shopping-cart text-cyan-400 text-2xl"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Customers Table -->
    <div class="glass-effect rounded-2xl overflow-hidden">
        <div class="p-6 border-b border-slate-800">
            <div class="flex items-center justify-between">
                <h2 class="text-xl font-bold text-white">All Customers</h2>
                <div class="flex items-center space-x-3">
                    <input type="text" placeholder="Search customers..." class="px-4 py-2 bg-slate-800/50 border border-slate-700 rounded-xl text-white placeholder-slate-500 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 transition-all">
                    <button class="px-4 py-2 bg-slate-800/50 border border-slate-700 rounded-xl text-slate-400 hover:text-white hover:border-slate-600 transition-all">
                        <i class="fas fa-filter"></i>
                    </button>
                </div>
            </div>
        </div>
        
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-slate-900/50">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Customer</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Contact</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Total Orders</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Joined</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-slate-400 uppercase tracking-wider">Status</th>
                        <th class="px-6 py-4 text-right text-xs font-semibold text-slate-400 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-800">
                    @forelse($customers as $customer)
                    <tr class="hover:bg-slate-900/30 transition-colors">
                        <td class="px-6 py-4">
                            <div class="flex items-center space-x-4">
                                <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-500 rounded-full flex items-center justify-center shadow-md">
                                    <span class="text-white font-bold text-lg">{{ substr($customer->name, 0, 1) }}</span>
                                </div>
                                <div>
                                    <p class="font-semibold text-white">{{ $customer->name }}</p>
                                    <p class="text-sm text-slate-400">ID: #{{ str_pad($customer->id, 4, '0', STR_PAD_LEFT) }}</p>
                                </div>
                            </div>
                        </td>
                        <td class="px-6 py-4">
                            <p class="text-white font-medium">{{ $customer->email }}</p>
                            <p class="text-sm text-slate-400">{{ $customer->phone ?? 'N/A' }}</p>
                        </td>
                        <td class="px-6 py-4">
                            <div class="flex items-center space-x-2">
                                <span class="px-3 py-1.5 bg-blue-500/20 text-blue-400 rounded-lg text-sm font-bold border border-blue-500/30">
                                    {{ $customer->orders_count }} orders
                                </span>
                            </div>
                        </td>
                        <td class="px-6 py-4">
                            <p class="text-white">{{ $customer->created_at->format('d M Y') }}</p>
                            <p class="text-sm text-slate-400">{{ $customer->created_at->diffForHumans() }}</p>
                        </td>
                        <td class="px-6 py-4">
                            <span class="px-3 py-1.5 bg-green-500/20 text-green-400 rounded-lg text-sm font-medium border border-green-500/30 flex items-center w-fit">
                                <span class="w-2 h-2 bg-green-400 rounded-full mr-2 animate-pulse"></span>
                                Active
                            </span>
                        </td>
                        <td class="px-6 py-4 text-right">
                            <div class="flex items-center justify-end space-x-2">
                                <button class="px-3 py-2 bg-blue-500/20 text-blue-400 rounded-lg hover:bg-blue-500/30 border border-blue-500/30 hover:border-blue-500/50 transition-all">
                                    <i class="fas fa-eye"></i>
                                </button>
                                <button class="px-3 py-2 bg-slate-700/50 text-slate-400 rounded-lg hover:bg-slate-700 border border-slate-600/50 hover:border-slate-600 transition-all">
                                    <i class="fas fa-ellipsis-v"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                    @empty
                    <tr>
                        <td colspan="6" class="px-6 py-12 text-center">
                            <i class="fas fa-users text-6xl text-slate-700 mb-4"></i>
                            <p class="text-slate-400 text-lg">No customers found</p>
                        </td>
                    </tr>
                    @endforelse
                </tbody>
            </table>
        </div>
        
        <div class="px-6 py-4 border-t border-slate-800">
            {{ $customers->links() }}
        </div>
    </div>
</div>
@endsection
